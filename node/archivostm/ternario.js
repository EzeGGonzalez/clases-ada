// CON TERNARIOS
const str = "A";
let resultado = str === "A" ? 1 : -1; // muestra 1

// CON IF
let resultado;
if (str === "A") {
  resultado = 1;
} else {
  resultado = -1;
}

const str2 = "B";
console.log(str2 === "A" ? 1 : -1); // muestra -1